(ns amo.core)

(defn add-subscriber!
  "No op. Dev experience for cljc only"
  [& _args])

(defn remove-subscriber!
  "No op. Dev experience for cljc only"
  [& _args])

(defn transact!
  "No op. Dev experience for cljc only"
  [& _args])