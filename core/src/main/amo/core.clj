(ns amo.core
  (:require
   [clojure.spec.alpha :as s]
   [amo.specs :as specs]
   [amo.protocols :as p]))

(defn add-subscriber!
  "No op. Dev experience for cljc only"
  [& args])

(defn remove-subscriber!
  "No op. Dev experience for cljc only"
  [& args])

(defn transact!
  "No op. Dev experience for cljc only"
  [& args])